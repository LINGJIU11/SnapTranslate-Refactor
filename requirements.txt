# SnapTranslate — Python 依赖（与原版保持一致，新增分层重构后未引入任何新依赖）
# 安装: pip install -r requirements.txt

# infrastructure 层 — 剪贴板、HTTP 翻译
pyperclip>=1.8.2
requests>=2.31.0
pillow>=10.0.0
pytesseract>=0.3.10

# infrastructure 层 — DeepSeek API（OpenAI 兼容协议）
openai>=1.40.0

# presentation 层 — Streamlit 网页版复习
streamlit>=1.33.0
