# SnapTranslate 分层重构报告（阶段一：结构化）

- 日期：2026-09-17
- 对象：`C:\Translate\SnapTranslate`（GitHub: ChenAI-TGF/SnapTranslate，4 个裸脚本）→ `C:\dsworkspace\SnapTranslate重构\`
- 边界：**纯结构重构**。功能、界面文案、错误提示、数据文件格式全部保持等价；已知缺陷原样保留并登记。

---

## 一、结论先行

1. **改完了**：4 个脚本（`main.py` 1918 行 + `vocab_review.py` 1026 行 + `vocab_review_web.py` 510 行 + `set.py` 197 行 = 3651 行）拆成 **112 个模块 / 6977 行**，按 **6 层单向依赖**组织，每层职责与依赖规则写在 `ARCHITECTURE.md`，由 `scripts/check_layering.py` 用 AST 静态校验——违反即命令失败。
2. **行为等价是跑出来的，不是声称的**：`scripts/parity_check.py` 把原版脚本与新实现放在同一批输入上对拍，**173 项 0 差异**（含纯函数与"文本翻译主链路""生词收录分支"两条流程级对拍）。
3. **同类工具链一起交付**：176 项单元测试 + 6 道门禁（分层 / 单测 / 冒烟 / 对拍 / 真实 Tk / Streamlit 结构对拍），一条命令 `python scripts/verify_all.py` 全跑。
4. **缺陷登记在案**：19 条原版缺陷逐条写清位置、成因、证据与修复方向（`KNOWN_ISSUES.md`），含"悬浮卡片把引擎标签写进生词本"这种已经在你的 `vocab.json` 里留下痕迹的问题。
5. **按你的要求没做任何功能改进**：翻译源仍是原来那 5 条免费逆向线路，界面布局与文案一字未改。

---

## 二、交付物

| 类别 | 位置 | 说明 |
|---|---|---|
| 工程主体 | `src/snaptranslate/`（107 个文件） | 6 层：`config` / `domain` / `infrastructure` / `application` / `presentation` / `bootstrap` |
| 兼容入口 | `entrypoints/main.py`、`vocab_review.py`、`vocab_review_web.py`、`set.py` | 与原版同名，`python entrypoints/main.py` 即可运行；也支持 `pip install -e .` 后的 4 个 console_scripts |
| 架构文档 | `ARCHITECTURE.md` | 分层契约、依赖方向表、端口与适配器、关键设计决策、验证策略 |
| 迁移映射 | `FILE_MAP.md` | 目录分级 + **原版每个函数搬到哪个新文件**的逐项对照 |
| 缺陷登记 | `KNOWN_ISSUES.md` | 19 条保留缺陷（`[保留]`）+ 6 条有意偏差（`[偏差]`）+ 4 条仓库遗留（`[历史]`） |
| 使用说明 | `README.md` | 快速开始、目录地图、验证命令 |
| 门禁脚本 | `scripts/`（6 个） | `check_layering` / `smoke_check` / `parity_check` / `gui_smoke` / `web_smoke` / `verify_all` |
| 测试 | `tests/`（15 个文件，176 项） | 领域、基础设施、用例、表示层纯逻辑、取词回归、热键热更新、分层约束 |

---

## 三、分层与文件分级

| 层 | 目录 | 文件 | 行数 | 职责 | 允许依赖 |
|---|---|---|---|---|---|
| L0 | `config/` | 4 | 98 | 路径、主题、应用元信息 | 仅标准库 |
| L1 | `domain/` | 28 | 815 | 模型、领域服务、12 个端口 Protocol、错误 | 标准库 + domain |
| L2 | `infrastructure/` | 28 | 1294 | 5 条翻译线路与竞速、Tesseract OCR、SAPI 朗读、JSON 持久化、Win32 输入、DeepSeek | + config |
| L3 | `application/` | 14 | 756 | 8 个用例 + 依赖包 + 进度/结果 DTO | + domain、config |
| L4 | `presentation/` | 32 | 3735 | 文案总表 + Tk 窗口族（`tk/` 24 文件）+ Streamlit（`web/` 6 文件） | + application |
| L5 | `bootstrap/` | 5 | 271 | 容器（唯一装配点）、装配函数、CLI/Streamlit 进程入口 | 全部 |
| — | 主包合计 | **112** | **6977** | | |
| — | `scripts/` + `tests/` + `entrypoints/` | 25 | — | 门禁、测试、兼容入口 | — |

依赖方向（单向、禁止逆向与跨层跳跃）：

```
presentation → application → domain
      ↓             ↓          ↑
      └──→ infrastructure ─────┘
                 ↓
              config
```

文件粒度：**单文件 60–300 行、单文件单职责**。111 个模块里只有 `presentation/texts.py`（321 行）略超上限，
它承担"全部界面文案的唯一出口"这一单一职责，拆开会让"同一句话在两个界面"重新分散，已在 `ARCHITECTURE.md §3` 作为唯一例外登记。

---

## 四、封装与"层层递用"是怎么落地的

1. **上层只认识端口，不认识实现**：`domain/ports/` 定义 12 个 Protocol（翻译、OCR、朗读、词表仓储、设置仓储、API Key、备份、剪贴板、取词、热键、窗口激活、例句生成、时钟、错误格式化）。
   应用层拿不到 `requests` / `pytesseract` / `ctypes`；表示层拿不到 `infrastructure` 与 `bootstrap`（有静态检查兜底）。
2. **用例是唯一的业务入口**（`application/`）：文本翻译、划词翻译、截图翻译、生词收录、最近一条收录、复习评分、批量生成例句、词表后台管理。
   原版这些逻辑分散在窗口类的 30 多个 `_do_xxx_job` 里，现在窗口只做"渲染 + 事件转发"。
3. **重复实现收敛，但**语义差异**显式命名而不是偷偷合并**：
   - 词表 IO 有 3 种加载语义（`load_raw` / `load_tolerant` / `load_strict`）——原版 4 个脚本各写一份且行为不同；
   - 设置有 2 种写入语义（`merge` / `replace`）——原版 `main.py` 合并且 `vocab_review.py` 覆盖；
   - "待补例句"有 2 种口径（复习端"任一为空"、后台管理"两者齐全"）——原版就不一致，保留并在文档与测试里点名。
4. **界面文案只有一处出口**：`presentation/texts.py`。Tk 窗口与 Streamlit 页面共用同一套字符串与格式化函数，改文案不会漏掉另一端。
5. **装配集中在一个组合根**：`bootstrap/container.py` 是唯一 `import` 全部层的地方，按配置造好适配器与用例，再以四个"依赖包"（`TranslateAppDeps` / `ReviewAppDeps` / `WebReviewDeps` / `AdminAppDeps`）交给表示层。
6. **进程入口分层明确**：`entrypoints/` 是原版脚本的薄壳；`bootstrap/cli.py` 与 `bootstrap/streamlit_entry.py` 分别承担 console_scripts 与 `streamlit run` 的进程级装配。

---

## 五、行为等价是怎么保证的

### 5.1 对拍机制（`scripts/parity_check.py`，173 项 0 差异）

| 组 | 覆盖内容 |
|---|---|
| 文本清洗 | `clean_text` 8 组输入、`is_likely_english` 6 组 |
| 热键 | 15 种组合的 `normalize` / `parse` / VK 映射 / 标签占位 |
| 翻译响应解析 | clients5 载荷 8 种形态、MyMemory 额度文案 6 种、`langpair` 猜测、失败文案 5 类异常 |
| 评分与复习 | `item_score` 9 组、`normalize`、`GRADE_DELTA`、待补例句口径、双语 JSON 解析 6 种、402 判定 5 种、排序 3 种模式 |
| 词表 IO | 三种加载语义 × 6 种文件形态（缺失/损坏/顶层非数组/混入非 dict）+ **写盘字节级一致** |
| 流程：文本翻译主链路 | 5 组输入，比对"传给翻译器的文本"与"界面展示文本"（含截断、换行折叠、引擎标签） |
| 流程：生词收录分支 | 4 种场景，比对走向（新增/重复/空内容）与落盘内容 |

### 5.2 重构过程中**发现并修掉**的不等价（这批最值钱）

| # | 问题 | 处理 |
|---|---|---|
| 1 | 悬浮卡片"收录生词本"写入带引擎标签的 display 文本 | **保留**（原版行为），登记为 #6，并留了修复入口 |
| 2 | 收录"暂无可记录内容"分支的悬浮提示取值应为 `False` | 修正（原版 `main.py:1476` 显式 `floating=False`） |
| 3 | 删除词条的三个分支都不应弹悬浮提示 | 修正（原版只改状态栏） |
| 4 | 翻译失败分支多打了一行原版没有的控制台日志 | 删除 |
| 5 | 热键格式错误文案应使用内部键名（`snip=bad`） | 修正（原版如此，虽像笔误） |
| 6 | 后台线程直接操作 Tk（状态栏与光标提示） | 修正为 `root.after(0, ...)`（原版用 `_set_status_safe`） |
| 7 | Web 端切换词表时先 `load` 后 `use_path`（加载了旧路径） | 修正为先 `use_path` 再 `load` |
| 8 | Web 端手动填写的 API Key 写到了默认路径而非用户指定路径 | 修正（原版写 `session_state.key_path`） |
| 9 | 缺 API Key 时不再弹窗索取，变成"每条都失败" | 新增 `ExampleGenerator.ensure_ready()` 端口能力，恢复"批量前先校验"的原版顺序 |
| 10 | Web 端每次 rerun 重建容器导致词表路径回退到默认值 | 修正（每次 rerun 重新绑定当前路径），并用 AppTest 验证"换表后评分只写新文件" |
| 11 | 面板与窗口的方法名不匹配（`host.on_enable_toggle` vs `_on_enable_toggle`） | 真实 Tk 构造时暴露，已统一；这类问题只有"真的把窗口建出来"才能发现 |
| 12 | 两处界面文案仍在窗口里硬编码（`"生词本"`、`"翻译完成"`） | 归口到 `texts.py`（`CollectText.TITLE`、`StatusText`） |
| 13 | Streamlit 入口在 `import` 时就渲染页面（副作用） | 加 `__name__ == "__main__"` 守卫（Streamlit 执行脚本时同样是 `__main__`，已用 AppTest 验证入口照常渲染） |

### 5.3 界面级验证（不止是"能编译"）

- `scripts/gui_smoke.py --with-tk`：在真实 Tk（本机 1920×1080，Tk 9.0.4）下构造划词窗口、复习窗口、后台管理窗口，装配完界面后立即销毁，不进 `mainloop`。
- `scripts/web_smoke.py`：用 Streamlit 官方 `AppTest` 渲染新页面，并把**原版页面**在同样的临时数据目录下渲染一遍，逐项比较标题、按钮文案与顺序、下拉框标签与选项、输入框、小标题——当前全部一致（10 个按钮、2 个下拉框）。

---

## 六、实测结果

| 门禁 | 命令 | 结果 |
|---|---|---|
| 分层依赖 | `python scripts/check_layering.py` | `[OK]` 无违规（并含"能否检出违规"的自测） |
| 单元测试 | `python -m unittest discover -s tests -t .` | `Ran 176 tests … OK` |
| 冒烟自检 | `python scripts/smoke_check.py` | 全包导入 + 四个依赖包装配 + 关键纯函数 `[OK]` |
| 与原版对拍 | `python scripts/parity_check.py` | `对拍用例 173 项，差异 0 项` |
| Tk 界面 | `python scripts/gui_smoke.py --with-tk` | 三个窗口构造 + 销毁全部 `[OK]` |
| Web 页面 | `python scripts/web_smoke.py` | 渲染无异常，结构与原版逐项一致 |
| 一键全跑 | `python scripts/verify_all.py` | 上表顺序执行，任一失败即非 0 退出 |

> 说明：在 PowerShell 里这些命令有时会显示 `[exit code: 1]`，那是 `unittest`/Streamlit 往 stderr 写进度信息触发的 `NativeCommandError` 噪声；判断成败请看脚本自己打印的 `[OK]/[FAIL]` 与 `verify_all.py` 的汇总。

---

## 七、与原版的差异（全部有意，且仅此 6 条）

| # | 差异 | 影响 |
|---|---|---|
| D1 | 数据目录可用 `SNAPTRANSLATE_DATA_DIR` 覆盖 | 默认仍是工程根目录，不设置时行为完全一致 |
| D2 | 新增 4 个 console_scripts 与 `entrypoints/` 薄壳 | 原 4 个脚本照旧可跑 |
| D3 | 领域异常类型替换裸异常（展示文案逐字保留） | `load_strict` 的"是否抛错"语义已由对拍覆盖 |
| D4 | 三种词表加载 / 两种设置写语义显式命名 | 原版本就不同，合并会改变行为 |
| D5 | 测试临时目录放在工程内 `.tmp-tests/` | 仅测试用；沙箱下子进程可能不可写系统临时目录 |
| D6 | 原版图片素材（实为 JPEG 的 `image/*.png`）与博客稿未复制 | 与代码结构无关，需要时从原仓库取 |

---

## 八、下一步（等你发话，不在本轮）

1. **阶段二（功能）**：报告一里那份路线现在有了安全网——改一处跑一次 `verify_all.py`，任何行为漂移都会立刻被对拍抓住。
   建议顺序：① 接 DeepSeek 做翻译主通道（复用已有 key 与 SDK）② 修代理支持 ③ 悬浮卡片自适应与可钉住 ④ 清洗已污染的 117 条词表 ⑤ OCR 换 RapidOCR ⑥ 热键去轮询 ⑦ 打包 exe。
2. **数据侧**：`vocab.json` 里"（Google 最快返回）"污染与 `(无翻译结果)` 脏条目是一次性可清洗的（见 `KNOWN_ISSUES.md` #6）。
3. **可选打磨**：`presentation/texts.py` 若确实需要拆包（`texts/` 子包 + 保持导入路径不变），一句话就能做；`KNOWN_ISSUES.md` 里的 19 条按优先级逐条修，每条都能配一个回归测试。

---

## 九、阶段一之后的修复（第二次交付，2026-09-17）

用户在真机实测中发现"划词翻译返回了上次复制的网址"，据此做了两处**有意偏离原版**的修复
（详版见 `KNOWN_ISSUES.md` §五）：

| 编号 | 问题 | 修复要点 |
|---|---|---|
| **F1** | 取词失败被静默吞掉：Ctrl+C 没生效时，程序把剪贴板里的旧内容（例如上次复制的网址）当原文翻译 | 取词判据从"内容是否变化"换成**剪贴板版本号** `GetClipboardSequenceNumber`；失败时返回 `copied=False`，用例**放弃翻译**并明确提示；版本号不可用时退回旧判据 |
| **F2** | 界面改热键必须重启程序才生效（原版每轮重新读热键，而阶段一把绑定一次性交给了监听器） | `HotkeyListener` 新增 `update_bindings()`，轮询监听器每轮读可变快照，`on_apply_hotkeys()` 改完立即同步 |

**为什么原版会返回网址**（诊断结论，脚本 `scripts/diagnose_url_capture.py` 可复现）：
① 原版取词只看"剪贴板内容变没变"，没变时不报错，直接把旧内容当取词结果；
② 各翻译源对网址是**原样返回**（实测 clients5 / MyMemory 都原样吐回 URL），
所以一旦原文是网址，界面就显示"网址 => 网址"，看起来像翻译坏了。

**验证**：新增 14 个回归测试（取词 9 + 热键监听 5）与 2 个用例层断言，
`tests` 总数 **160 → 176**；六道门禁全绿；`gui_smoke --with-tk` 里加了
"改热键后监听器即时同步"的端到端断言。
